# Write a Python program to select an item randomly from a list.

import random

lst = ['apple', 'banana', 'cherry', 'date', 'fig']

randam_item = random.choice(lst)

print ("Random item selected form list is",randam_item)