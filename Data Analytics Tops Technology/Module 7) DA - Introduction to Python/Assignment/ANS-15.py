# Write a Python program to calculate the length of a string. 

# Get String form user
Str = input("Enter string : ")

# Calculate string length using 'len' function
length = len(Str)

# Print length of string
print(f"length of '{Str}' is {length}.")